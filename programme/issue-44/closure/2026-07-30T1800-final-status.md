# Issue #44 — final status (2026-07-30T18:00)

*Agent 45, sole publisher for this closure pass. No document moves published in this
pass — verification, classification, and closure only.*

## §1 — independently regenerated ledger

Regenerated from `/api/v1/changes` directly (136 published changes fetched in one call,
filtered for `MOVE_PAGE` operations), **not** by incrementing the prior total:

- **35 move-bearing publications.**
- **90 `MOVE_PAGE` operations.**
- **90 unique stable page IDs** — zero resource ID moved more than once.
- **Zero duplicates.**
- **Zero already-at-destination (no-op) moves.**
- Every resource ID's *last* (and only) recorded `to_path` matches its live current path
  exactly — zero mismatches.
- Cross-checked against the existing ledger file: **exact set match**, 90/90, all
  `current_path` claims confirmed.

Full detail: `independent-move-regen.json` (committed).

## §2 — three hub dispositions

- **`ccm.md`** — final path `control-plane/foundational/ccm.md`. Moved as a normative
  foundational contract (I-1..I-5). No artificial CCM subtree created; related `ccm-*`
  pages remain in their already-correct locations (design/, runbooks/, specs/).
- **`cp-net-1-driver.md`** — final path `control-plane/cp-net-1/driver.md`. Moved into
  the already-existing CP-NET-1 section alongside `overview.md`; not converted into a new
  landing page.
- **`hub2-authority-crosswalk.md`** — **retained at its current path**, deliberately. Nine
  inbound citations, unanimously, treat it as a navigational entry point — the same
  reader role as `control-plane/index.md`. No justified structural move or hub conversion
  exists. **Resolved without movement**, not unfinished migration debt.

## §3 — final structural disposition (248 tracked pages, every one resolved)

| Disposition | Count |
|---|---|
| moved under Issue #44 | 90 |
| correctly placed | 65 |
| archived | 42 |
| backlog/paused | 16 |
| separate follow-up | 13 |
| active project/board | 10 |
| deliberate non-move | 4 |
| evidence surface | 4 |
| authoring decision required | 2 |
| lifecycle decision | 1 |
| hold-blocked | 1 |
| **total** | **248** |

Zero pages remain in a generic or unexplained "still requires movement" category. The
prior baseline (v104 snapshot) carried three such buckets — `still requires movement` (22),
`reorganisation candidate pending semantic clustering` (22), and a 39-page unresolved
"reported by Agent 44" blob. Every one of those 83 pages was individually resolved this
pass: 36 had already moved between v104 and v146 (confirmed live, reclassified `moved`);
the remainder were assigned a specific final category using live lifecycle/status/nav_path
evidence (`backlog/paused` for `lifecycle=BACKLOG`, `active project/board` for
`lifecycle=ACTIVE`, `correctly placed` for already-sensible operations/ root tier docs,
`deliberate non-move` for entry-point pages, `lifecycle decision` for the one page with a
genuine metadata/content mismatch, `separate follow-up` for genuine ordinary-cohort
candidates outside this session's scope). Full detail, page-by-page:
`final-retrospective-classification.json` (committed).

## §4 — remaining work, separated into owned follow-ups

- **Absent-target authoring** ([#94](https://github.com/cpaumelle/docplane/issues/94)):
  16 unique scoped items (14 original + 2 newly surfaced this closure pass — both a
  direct, mechanical consequence of the `cp-net-1-driver.md` move correctly preserving
  its own blockquoted doc-structure note, not new authoring gaps; disposition table
  updated with the addendum). No pages created, no targets guessed.
- **CCM capability lifecycle** ([#95](https://github.com/cpaumelle/docplane/issues/95)):
  `ccm-interface-realization-capability.md` needs a content-authority decision (body vs.
  metadata mismatch), not a mechanical move.
- **CP-NET-1 history** ([#96](https://github.com/cpaumelle/docplane/issues/96)):
  `cp-net-1-history.md` is a natural companion move, deliberately not folded into this
  session's ledger — scoped as its own future publication.
- **API-native publication lease** ([#97](https://github.com/cpaumelle/docplane/issues/97)):
  design already committed; required before concurrent publication resumes, but not a
  completion condition for the corpus reorganisation itself.

## §5 — final audits

- **Corrected full-corpus internal-link audit**: run with the merged canonical resolver
  (`docplane#91`) — confirmed after catching and correcting a real process error (the
  first run used a local clone still checked out on `programme/issue-44-shared-record`,
  which predates the resolver-fix merge; caught via a discrepancy against the independent
  verifier, corrected by switching to `main`, rerun clean).
- **Stale Issue #44 old-path audit** (all 90 ledger entries, full corpus): **zero pages
  need rewriting.** 34 raw preservation records remain — all individually traced to
  already-known evidence-surface or blockquote-protected pages (`operations/incidents/*`,
  `archive/plans/c1-implementation-plan.md`, `shadow-inventory.md`, `cp-net-1-history.md`,
  `cp-net-1/overview.md`, `w4-1-5c-g1a-evidence.md`, `pi4-gateway-appliance-v1-inventory.md`,
  `invariants/index.md`). Exact match against a from-scratch independent verifier (34/34,
  zero discrepancy).
- **Target-existence audit / owned-exception reconciliation**: 16 scoped absent targets,
  every one dispositioned (§4).
- **Route uniqueness check**: zero duplicate rendered routes across the full 521-page
  active corpus.
- **Page-vs-section conflict check**: zero conflicts.
- **Certification**: `CURRENT`, `state_version` 146.
- **Working equals deployed**: confirmed.
- **Zero open changes**: confirmed.

## §6 — final statement

Baseline retrospective total: 248 pages tracked under Issue #44's `control-plane/`+
`operations/` scope (a current-state retrospective count, not a literal original-day
denominator — no percentage is published against an invented baseline).

**Issue #44 structural reorganisation is complete. Ninety unique pages were moved, all
identified ordinary cohorts and assigned hub decisions were resolved, and no unexplained
structural migration candidates remain in scope. Remaining work consists of separately
owned authoring, lifecycle and platform follow-ups.**

- Unique pages moved: **90**.
- Pages resolved without movement: **4** (`hub2-authority-crosswalk.md`,
  `control-plane/index.md`, `control-plane/architecture-overview.md`, plus one more entry
  — see full table).
- Pages excluded by lifecycle or evidence status: **46** (42 archived + 4 evidence
  surface).
- Authoring/lifecycle follow-ups: **3 issues** (#94, #95, #96) plus the platform lease
  (#97).
- Structural pages remaining in scope: **zero.**
- Current certification: **v146, CURRENT, working==deployed, zero open changes.**
