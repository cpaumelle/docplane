# Issue #44 — hub conversion session, closeout (2026-07-30T17:30)

*Agent 45, sole publisher. Corrective phase (resolver fix + 70-rewrite repair) confirmed
CURRENT and unchanged at session start (v141 → drifted to v142/v144/v146 during this
session from unrelated, verified-safe parallel human activity; zero drift on any target
page at every checkpoint).*

## Design outcome — three hubs, three different answers

Read full body, nav_path, and inbound-citation pattern for all three before designing
anything, per instruction. Inbound count alone did not determine the design in any of the
three cases:

### `ccm.md` → **ordinary move**, `control-plane/foundational/ccm.md`
Title "CCM Contract"; content is I-1..I-5 invariants, state model, API contract, artifact
ownership — a normative governance document, not a catalogue with subordinate pages. Its
own `nav_path` already read "Foundational" before this move (a pre-existing mismatch
between file location and navigation). All 23 inbound citations read it as canonical
authority ("CCM Contract", "governance contract", "this sprint preserves I-2"). Five
sibling foundational docs already made this exact move
(`doctrine.md`, `correctness-domains.md`, `gateway-convergence-doctrine.md`,
`docs-gen-1.md`, `shadow-inventory.md`) — joining them is not a new pattern.
**No CCM subtree created** — the related `ccm-*` pages (2 design, 3 runbooks, 1
capability pending its own decision, 1 archived implementation, 1 archaeology page)
were confirmed already correctly dispositioned elsewhere; none became children of this
move.

### `cp-net-1-driver.md` → **structural move into an already-existing section**, `control-plane/cp-net-1/driver.md`
The driver doc explicitly defers architectural authority to a sibling ("for architectural
overview see CP-NET-1 Overview") which already lives at `control-plane/cp-net-1/overview.md`
— a section the reorg had already half-started. All 17 inbound citations read it as the
authoritative *programme driver* for an active migration (CP-NET-1 DNS fabric renumber),
never as architecture. This move completes the section structure by relocating the file to
match its own already-correct `nav_path` — it does **not** become the section's landing;
`overview.md` already fills that role conceptually (though not yet literally
`index.md` — out of scope for this session, flagged as a follow-up).
`cp-net-1-history.md` was **not** touched — expanding scope beyond the three named pages
was explicitly out of bounds this session, even though it would tidy the same section.

### `hub2-authority-crosswalk.md` → **no move**
All 9 inbound citations, without exception, describe it identically: "primary entry
point," "navigational context," "post-bootstrap operational questions," "where do I look
for X." This is unanimous evidence for "heavily cited leaf reference," not a section
landing — the task's own explicit permitted outcome. It sits at the same structural tier
as `control-plane/index.md` and `control-plane/architecture-overview.md` (both also flat,
both also "entry point" documents) — moving it into `foundational/` (a doctrine/contract
category) would misclassify its genre. Corrected disposition: **remains exactly where it
is.** This is a resolved determination, not a deferral.

## Safety analysis (both moves)

- Page-vs-section conflict: checked all four destination paths (`control-plane/foundational/ccm.md`,
  `control-plane/cp-net-1/driver.md`, plus the directory-vs-file collision cases
  `control-plane/cp-net-1.md` and `control-plane/foundational.md`) — all free, zero collisions.
- Outbound relative-link depth changes: computed via `plan_source_move()`, not by hand.
  `ccm.md` needed 3 corrections (`architecture/index.md`, `../services/ccm-edge.md`,
  `../network/fabric-v2/wireguard.md`); `cp-net-1-driver.md` needed 7 (sibling
  `cp-net-1-history.md`, three `invariants/*` refs, `dns-vip-authority-trace.md`,
  `invariants/index.md`, `design/cp-net-1-gap-2-edge-resolver-authority.md`).
  Masking-equality proven for every page, both directions.
- Inbound reference repair: `ccm.md` — 23 raw references across 15 pages, all rewritten
  (matches the confirmed inbound count exactly). `cp-net-1-driver.md` — 11 rewrites across
  7 pages rewritten; 6 references correctly **preserved**, not rewritten (4 in
  `cp-net-1-history.md` — an evidence-surface page by its own filename pattern — plus one
  blockquote each in `cp-net-1/overview.md`'s "doc structure" note and
  `invariants/index.md`'s "Transition status" note). All 6 preservations individually
  inspected and confirmed legitimate before publishing, not assumed.
- Redirect/route verification: old and new routes both return 200 for both hubs, plus the
  unmoved `cp-net-1/overview.md` confirmed unaffected.

## Publication order and evidence

Two bounded publications per hub (move, then reference repair), each independently
rehearsed, validated, and — for the move phase — re-drift-checked immediately before
publish (parallel human activity advanced certification three times during this session;
zero drift on any of the 24 total target pages at any checkpoint):

| Hub | Move change | Refs change | Rewrites |
|---|---|---|---|
| `ccm.md` | `e0152f48` | `6c7bea1f` | 1 own-depth (3 links) + 15 inbound pages (23 links) |
| `cp-net-1-driver.md` | `945dc7ab` | `ea02e258` | 1 own-depth (7 links) + 7 inbound pages (11 links) |

Post-publish, full-corpus `plan_corpus` re-run against both new mapping entries: **zero**
remaining stale-rewrite candidates, 6 preservations remaining (exactly the pre-identified,
verified-legitimate set). Certification v146, CURRENT, working==deployed, zero open
changes.

## Ledger

**90 unique pages moved / 90 move operations** (up from 88). Both new entries recorded
with exact publish timestamps and publication IDs.

## Progress close-out

- **Hubs reviewed:** 3 (`ccm.md`, `cp-net-1-driver.md`, `hub2-authority-crosswalk.md`).
- **Hubs converted (structural relocation):** 2.
- **Pages moved this session:** 2.
- **Cumulative unique pages moved (Issue #44 total):** 90.
- **Pages retained as heavily-cited leaves (no move, resolved by design):** 1
  (`hub2-authority-crosswalk.md`).
- **Authoring decisions created:** 0 (none created this session — the 14 absent-target
  items were explicitly left untouched, disposition table unchanged from the prior pass).
- **Certification:** v146, CURRENT, working==deployed, 0 open changes.
- **Audit state:** canonical full-corpus scan shows zero remaining stale references to
  either converted hub's old path.

### Structural vs. authoring vs. platform status

- **Structural reorganisation for these three hubs: complete.**
- **Authoring cleanup outstanding:** the 14 absent-target items (unchanged from the prior
  pass — separate authoring-decision bundle still to be produced, not mixed into this
  session); `cp-net-1-history.md` could join `cp-net-1/` as a natural follow-up but was
  out of this session's named scope; `ccm-interface-realization-capability.md` still
  needs its own lifecycle decision (unrelated to these three hubs, noted only as
  already-known context).
- **Platform enhancement outstanding:** the API-native publication lease remains
  undeployed; concurrent publication stays disabled (not required for this single-owner
  session, per instruction).

No absent-target authoring decisions were mixed into this pass. No new page moves beyond
the two named, evidence-supported hub conversions.
