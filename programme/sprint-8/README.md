# Sprint 8 — coverage burn-down

`meter-list-service-map-suggestions.json` is a read-only, names-only report
generated by:

```bash
python3 scripts/meter_list.py --suggest-services
```

It was generated against the 256-rule `hub2.prometheus` source at fingerprint
`b9b1f94d0557039ff2761d114b31b92d53f99687881cf099c39a025b15cc8834` and
contains the 164 rules without upstream service labels. Candidate service
entity keys and fuzzy scores are curation evidence only. They create no
mapping or `WATCHES` link; an operator must confirm real ownership and add a
versioned overlay entry, or preferably fix the upstream rule label.
