# docs-api (vendored)

Verbatim copy of the upstream docs-api application code (FastAPI control
plane). Refresh with `../scripts/sync-from-upstream.sh` when this directory
lives inside the upstream repo; otherwise this IS your source — edit freely.

See the top-level README for usage, and note that `app/lint_rules.py` ships
with the upstream project's rules as worked examples — replace them with
your own.
