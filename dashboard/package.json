{
  "name": "docplane-dashboard-assets",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.js",
    "check": "node --check esbuild.config.js && node --check editor-src/editor.js && node --check static/authoring.js"
  },
  "dependencies": {
    "@codemirror/lang-markdown": "6.5.1",
    "codemirror": "6.0.2"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
