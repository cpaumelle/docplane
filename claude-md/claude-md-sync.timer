[Unit]
Description=Hourly sync of CLAUDE.md from the docs API

[Timer]
# Fire 2 minutes after boot, then hourly.
OnBootSec=2min
OnUnitActiveSec=1h
# Spread across machines so they don't all hit the API at :00.
RandomizedDelaySec=5min
Persistent=false

[Install]
WantedBy=timers.target
